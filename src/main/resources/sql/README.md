# Legacy SQL Reference

Flyway is the schema owner. Do not mount these SQL files for database initialization.

The files in this directory are retained only as historical reference for the pre-Flyway schema. Runtime schema changes must be added under `src/main/resources/db/migration`.
