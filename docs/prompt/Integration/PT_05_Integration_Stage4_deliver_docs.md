继续当前上下文，不要重新分析项目结构，不要重写代码。

当前 Price Tracker 项目已经完成端到端联调，并成功跑通完整链路：

注册/登录 → 创建商品 → 添加关注 → 手动刷新价格 → RabbitMQ Producer → RabbitMQ Consumer → 写入 tb_notification

现在进入 Integration 收口阶段的最终交付任务。

你的任务不是新增功能，而是把“已经跑通的系统”固化为可复现、可验证、可交付的工程资产。

---

【任务 1：固化端到端流程（最高优先级）】

请基于当前实际代码，实现一份可执行的 README 补充内容，要求：

1. 用 curl 或 HTTP 示例描述完整链路：

    * 注册用户
    * 登录获取 token
    * 创建商品
    * 添加关注（设置 target price）
    * 手动触发价格刷新
2. 每一步必须包含：

    * 请求路径
    * 请求参数示例
    * 响应示例
3. 明确验证方式：

    * MySQL：price_history / tb_notification
    * RabbitMQ：queue 消息
    * 日志关键输出
4. 所有内容必须与当前代码实现一致，不允许虚构

---

【任务 2：补充异常与边界测试场景】

请新增“手动验证场景”文档，至少包含：

1. 未达到目标价

    * 不应发送 MQ
2. 同一价格重复刷新

    * 不应重复通知（幂等）
3. MQ 消费异常（模拟抛异常）

    * 是否有日志或重试机制
4. 非法商品 ID

    * 是否返回规范错误

每个场景必须包含：

* 操作步骤
* 预期结果
* 实际验证方式

---

【任务 3：系统链路说明（用于面试）】

请补充一段简洁的系统说明文档，包括：

1. 业务链路（用户 → 通知）
2. 技术链路（Controller → Service → DB → MQ → Consumer）
3. 核心设计点：

    * Redis 的作用
    * RabbitMQ 的作用
    * 幂等处理思路
    * 日志如何辅助排查

要求：

* 面向面试表达
* 简洁清晰，不要长篇废话

---

【约束（必须遵守）】

1. 不要新增业务功能
2. 不要修改核心业务逻辑
3. 不要重构项目结构
4. 所有内容必须基于现有实现
5. 优先修改 README.md 或 docs 目录
6. 输出：

    * 修改了哪些文件
    * 每个文件新增了什么内容
    * 如何验证这些文档是正确的

---

【目标】

让一个完全不了解项目的人：

1. 按 README 能跑通系统
2. 能验证完整链路
3. 能理解系统设计

请直接开始执行，不要再次询问确认。
